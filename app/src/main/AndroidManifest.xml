<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.airmonitor">

    <!-- Required only when requesting background location access on
   Android 10 (API level 29) and higher. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AirMonitor">
        <activity
            android:name="com.airmonitor.views.AirQualityActivity"
            android:exported="true"
            android:label="@string/title_activity_air_quality_navigation"
            android:theme="@style/Theme.AirMonitor.NoActionBar" />
        <activity
            android:name="com.airmonitor.SignUpActivity"
            android:exported="true"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/Theme.AirMonitor.NoActionBar" />
        <activity android:name="com.airmonitor.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>



</manifest>